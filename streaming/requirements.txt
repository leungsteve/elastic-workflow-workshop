# Streaming module dependencies
# These are a subset of the main requirements.txt

# Elasticsearch
elasticsearch>=8.0.0,<9.0.0
elasticsearch[async]>=8.0.0,<9.0.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0
