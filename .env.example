# Elasticsearch Connection
# URL format varies by deployment type:
#   - Cloud Serverless: https://<deployment-id>.es.<region>.gcp.elastic.cloud:443
#   - Cloud Hosted:     https://<deployment-id>.<region>.gcp.elastic-cloud.com:443
ELASTICSEARCH_URL=https://your-cluster.es.region.gcp.elastic.cloud:443

# Kibana Connection (for Agent Builder API, Workflows API, etc.)
# URL format varies by deployment type:
#   - Cloud Serverless: https://<deployment-id>.kb.<region>.gcp.elastic.cloud:443
#   - Cloud Hosted:     https://<deployment-name>.kb.<region>.gcp.elastic-cloud.com:443
# NOTE: Kibana URL often has a DIFFERENT prefix than ES URL on Cloud Hosted
#       Check the Cloud console for the exact Kibana endpoint
KIBANA_URL=https://your-cluster.kb.region.gcp.elastic.cloud:443

# Authentication
# API Key works for both Elasticsearch and Kibana on Cloud deployments
ELASTICSEARCH_API_KEY=your-api-key-here

# For Local/VM deployments - use username/password instead:
# ELASTICSEARCH_USERNAME=elastic
# ELASTICSEARCH_PASSWORD=changeme

# Application Settings
APP_ENV=development
APP_PORT=8000
LOG_LEVEL=INFO

# ELSER Inference (optional - semantic search)
# Verify ELSER availability in your environment before enabling
ELSER_INFERENCE_ID=elser
ELSER_ENABLED=false

# Target Business Override (optional)
# Set this to skip automatic target selection
# TARGET_BUSINESS_ID=yelp-biz-xxxxx
